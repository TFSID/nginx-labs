services:
  nginx-vuln:
    build: .
    container_name: ngixshell-lab
    cap_add:
      - SYS_PTRACE
    security_opt:
      - seccomp=unconfined
    init: true
    ports:
      - "19321:19321"
    tty: true
    stdin_open: true
    restart: unless-stopped
